package com.stardustpvp.hud;

/** Snapshot of keyboard/mouse state for a visual-only Keystrokes HUD. */
public final class KeystrokesState {
    public boolean forward;
    public boolean left;
    public boolean back;
    public boolean right;
    public boolean jump;
    public boolean leftMouse;
    public boolean rightMouse;
}
